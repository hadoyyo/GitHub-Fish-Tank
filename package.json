{
  "name": "github-fish-tank",
  "version": "1.0.0",
  "description": "Animated aquarium showing GitHub language stats as fish",
  "main": "api/generate.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}